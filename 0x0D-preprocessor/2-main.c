#include <stdlib.h>

/**
 * main - prints the name of the file it was compiled from
 * followed by a new line
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	_putchar(__LINE__);
	_putchar('\n');

	return (0);
}
