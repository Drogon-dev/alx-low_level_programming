#include "main.h"

/**
 * _islower - checks if a character is lowercase
 * @c - arguments to be passed
 * Return: 1 if true, 0 if otherwise
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
