#include <stdio.h>

/**
 * main - prints out programming is like  building a multilingual puzzle
 * Return: 0 if executed properly or non-zero otherwise
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
